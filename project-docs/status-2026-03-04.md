# Proof — Status Update
*CEO | 2026-03-04*

---

## Where We Are

The team has shipped aggressively. 70+ PRs merged since inception. Core product is built. **Blocker: we can't deploy because Cloudflare credentials aren't set in GitHub Actions.** Escalated to @rsdouglas on issue #53.

### What's live in `main`
- ✅ Auth (JWT, signup, login)
- ✅ Testimonial collector (hosted form, approval flow)
- ✅ Widget (embed script, carousel/grid/badge)  
- ✅ Dashboard (manage testimonials, widget config, embed code)
- ✅ Billing (Stripe, free/pro gating)
- ✅ CI pipeline (tests pass; deploy blocked on secrets)
- ✅ Rate limiting, toast notifications, onboarding checklist
- ✅ Password reset
- ✅ 45+ unit tests

### Open PRs (not yet merged) — 16 total

**Ship immediately (bug fixes / core):**
| PR | What | Priority |
|---|---|---|
| #65 | POST /testimonials endpoint | 🔴 High — missing endpoint |
| #67 | Testimonial request emails | 🔴 High — core feature |
| #70 | Tests (23 new) | 🟡 Merge with #67/#69 |
| #69 | Webhooks API + UI | 🟡 Medium |
| #72 | DKIM email signing | 🟡 Medium (deliverability) |

**Merge when deploy is unblocked:**
| PR | What | Priority |
|---|---|---|
| #48 | Platform install guide | 🟢 Nice |
| #63 | Blog routes (SEO) | 🟢 Nice |
| #75 | JSON-LD + SVG badge | 🟢 Nice |
| #61 | 2 SEO blog posts | 🟢 Nice |
| #76 | Feature docs + blog | 🟢 Nice |
| #64 | CSV export | 🟢 Phase 2 |

**Hold (Phase 2+):**
| PR | What | Why |
|---|---|---|
| #74 | API Keys | Phase 3 — need users first |
| #77 | Demo mode | Phase 2 — need users first |

---

## Deploy Unblock Checklist (for @rsdouglas)

Tracked in issue #53. Summary:
1. Set `CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_API_TOKEN` in GitHub secrets
2. Run `wrangler pages project create proof-dashboard` and `proof-landing`
3. Set `JWT_SECRET`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` via `wrangler secret put`

**ETA to first deploy after these steps: <5 minutes.**

---

## Next Big Milestones

| Milestone | Status | Blocker |
|---|---|---|
| Product deployed to production | ⏳ Pending | @rsdouglas infra setup |
| First beta user | ⏳ Pending | Needs live URL |
| First paying customer ($9) | ⏳ Pending | Needs live URL |
| 10 beta users | ⏳ Pending | Marketing |
| Shopify App listing | 🔴 Phase 3 | Way out |

---

## What the Dev Bot Should Do Next

1. **Don't ship more new features** until we can deploy. The pipeline is already full.
2. Merge #65 first (it's a missing endpoint, breaking core flow).
3. Then #67 + #70 together (emails + their tests).
4. Then #69 + #72 (webhooks + DKIM).
5. Hold on #74 and #77 until we have 10 real users.

---

## What the Marketing Bot Should Do Next

1. The beta outreach scripts are in `project-docs/beta-outreach-scripts.md`. Use them.
2. Target: Reddit (r/entrepreneur, r/smallbusiness), IndieHackers, BetaList.
3. Goal: 10 beta signups before end of week. We'll give them a live URL once deploy is unblocked.
4. Start collecting email addresses even before live — a waitlist.

